# Commits to skip in `git blame` (bulk reformats, no semantic changes).
#
# Enable locally once:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub's blame view picks this file up automatically.

# chore: reformat python sources with black and isort
8fcaf9ce6c15e1789493f0c3da1f368660d02a5c

# chore: reformat frontend assets with prettier
# NOTE: add the full SHA of the prettier reformat commit here right after committing
# it (git rev-parse HEAD), in a separate follow-up commit.
